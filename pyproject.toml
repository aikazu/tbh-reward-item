[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "tbh-desktop"
version = "0.0.0"
description = "TBH Desktop App"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["."]
include = ["dev_tools*"]
