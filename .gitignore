# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.coverage
.coverage.*
htmlcov/

# mitmproxy CA (never commit private keys / local CA)
.mitmproxy/
*.pem
*.p12
!mitmproxy-ca-cert.pem.example

# OS / editor
.DS_Store
Thumbs.db
*.swp
*.swo
*~

# Local config (may contain port preferences, debug toggles)
*.local.json

# tbh.city scrape caches (regenerated by "Scrape data" button —
# regenerated content is local-only and shouldn't be committed).
tbh_desktop/box_loot_cache/
tbh_desktop/gear/
tbh_desktop/images/
tbh_desktop/item/
tbh_desktop/stages/
tbh_desktop/items_normalized.json
tbh_desktop/items_index.json
tbh_desktop/stages_index.json
tbh_desktop/stage_drop_map.json
tbh_desktop/pool_drops.json
tbh_desktop/manifest.json
tbh_desktop/box_slug_cache.json
tbh_desktop/drops_index.json

# Auto-generated / GUI / OS
*_autosave.py
*.desktop